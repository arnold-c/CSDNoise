# Laptop monitor
# update_theme!(; size = (1220, 640))
# update_theme!(; size = (2100, 1200)) # Home monitor
# update_theme!(; size = (950, 550)) # Work monitor
# CairoGLMakie.activate!(; type = "png", px_per_unit = 1.0)
